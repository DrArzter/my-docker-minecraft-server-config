# my-docker-minecraft-server-config

This is my configs for [minecraft server deployed in docker](https://github.com/itzg/docker-minecraft-server)

To use, just copy everything to root of the project mentioned and overwrite the original docker-compose file.